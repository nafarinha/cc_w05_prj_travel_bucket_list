<h1>Cities</h1>

<table>
  <tr>
    <th>Cities</th>
    <th>Countries</th>
  </tr>
  <% for city in @cities %>
  <tr>
    <td><a href="/cities/<%= city.id %>"><%= city.name %></a></td>
    <td><%= city.country.name %></td>
  </tr>
  <% end %>
</table>
